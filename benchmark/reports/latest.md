# Search benchmark summary

_No live eval run committed yet._

Generate this file:

```bash
# After ingest + migration 040
npm run benchmark:eval
```

Unit tests (no database):

```bash
npm run test:benchmark
```
