/**
 * @deprecated REMOVED — This file is intentionally empty.
 *
 * The 'partner' source was based on a non-existent GitHub organization (github.com/mcp)
 * and has been decommissioned. The concept of canonical/authoritative servers is now
 * handled by the `is_canonical` boolean field in the DB, populated from Smithery's
 * `bySmithery: true` signal during ingest.
 *
 * This file will be deleted in the next cleanup pass.
 */

export {};
